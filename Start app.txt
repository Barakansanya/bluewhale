# Restart PostgreSQL
sudo service postgresql restart

# Backend setup
cd server
npm install
npx prisma migrate dev --name init
npx prisma generate
npm run prisma:seed
npm run dev

# Frontend setup (new terminal)
cd ../client
npm install
npm run dev

#Github 
# 1. Make changes to your code...
# 2. Add the changes to be committed
     git add .
# 3. Commit the changes with a clear message
     git commit -m "feat: describe what you changed"
# 4. Push the changes to GitHub
     git push

NEXT ON LIST
Perfect! All works like a charm:)Yaay! Now let's clean out the UI/UX design, Starting from the login page all the way through the app.
Meaning elevate the login page to match the WOW effect app theme as well. One more thing, the logo is not accurate at all please see the attached image to refresh your memory.
Apply the fresh logo accurately to the app where necessary. Do that with precision like Jobs,Torvald and Musk sharpnesses combined.Impress them!
Finish this quick hardle the best be prepared for "Real data Integration".

Once done, the Data Scrapper implementation
 
 