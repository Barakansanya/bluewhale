# Restart PostgreSQL
sudo service postgresql restart

# Backend setup
cd server
npm install
npx prisma generate
npx prisma migrate dev --name init
npm run dev

# Frontend setup (new terminal)
cd ../client
npm install
npm run dev